let chai = require("chai");
let assert = chai.assert;

describe("Difference between items in 2 arrays" () => {
    it("should return an array", () => {
      const result = findDiff()
      assert.equal(Array.isArray())
    });

    it("should return an array of the items unique to each array", () =>)
})

//The above stuff is incomplete.  My app.js solution works, however.
